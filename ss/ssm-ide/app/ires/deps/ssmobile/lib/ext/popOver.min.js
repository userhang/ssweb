/*! ssmobile 2014-02-18 */
!function (a) {
    a.ssm && a.ssm.addExtension(function () {
        var b = function (b) {
            function c() {
                a(".POPOVER").animate({opacity: 0}, 500), setTimeout(function () {
                    a(".POPOVER").remove()
                }, 1e3)
            }

            var d, e, f = new Object, g = event.target, h = g.offsetTop, i = g.offsetLeft, j = g.offsetHeight, k = g.offsetWidth, l = {
                height: "80",
                width: "150",
                to: "top",
                interval: "4000"
            }, m = a.extend({}, l, b);
            "top" == m.to ? (d = h + j + 10, e = i) : "right" == m.to ? (d = h, e = i - m.width - 20) : "bottom" == m.to ? (d = h - m.height - 20, e = i) : "left" == m.to && (d = h, e = i + k + 10);
            var n = a("<div CLASS='POPOVER' style=\"height:" + m.height + ";width:" + m.width + ";top:" + d + ";left:" + e + "\"><div CLASS='CONTENT'></div><s CLASS=" + m.to + "><i></i></s></div>");
            return f.elemContent = n.children(".CONTENT"), n.appendTo("#ssm > .CURRENT"), a(".POPOVER").show(), setTimeout(c, m.interval), a(".POPOVER").on("tap", function () {
                c()
            }), f.setContent = function (a) {
                this.elemContent.text(a)
            }, f
        };
        return publicObj = {popOver: b}
    })
}(Zepto);